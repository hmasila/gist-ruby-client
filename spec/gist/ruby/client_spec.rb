RSpec.describe Gist::Ruby::Client do
  it "has a version number" do
    expect(Gist::Ruby::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
